# ==================== Phone Agent 核心依赖 ====================
# 主要Python版本要求
Python>=3.8,<3.12

# 基础库
tkinter                   # Python标准库，GUI基础
threading                 # Python标准库，多线程
os                        # Python标准库，操作系统接口
sys                       # Python标准库，系统接口
datetime                  # Python标准库，日期时间
re                        # Python标准库，正则表达式
json                      # Python标准库，JSON处理
queue                     # Python标准库，队列
atexit                    # Python标准库，退出处理
warnings                  # Python标准库，警告处理
contextlib                # Python标准库，上下文管理
traceback                 # Python标准库，异常追踪

# ==================== 第三方GUI库 ====================
customtkinter==5.2.2      # 现代自定义tkinter界面
tkinter                   # 基础GUI库（Python自带）

# ==================== AI和API库 ====================
zhipuai==2.0.0            # 智谱AI官方SDK
openai==1.12.0            # OpenAI兼容接口（如果需要）
requests==2.31.0          # HTTP请求库

# ==================== 语音合成和音频处理 ====================
torch==2.1.0              # PyTorch深度学习框架
torchaudio==2.1.0         # PyTorch音频处理
numpy==1.24.3             # 数值计算
soundfile==0.12.1         # 音频文件读写
pyaudio==0.2.13           # 音频播放和录制
wave                     # Python标准库，WAV文件处理

# ==================== 手机控制和ADB ====================
adbutils==2.7.7           # ADB工具Python封装
pure-python-adb==0.3.0    # 纯Python ADB实现,可能过时，adbutils已经足够
scrcpy-client==1.5.0      # 可能不存在，改用系统安装的scrcpy

# ==================== 数据处理和工具 ====================
pandas==2.0.3             # 数据分析（可选）
numpy==1.24.3             # 数值计算（已列在音频处理部分）
Pillow==10.1.0            # 图像处理

# ==================== 系统工具 ====================
psutil==5.9.6             # 系统监控
pywin32==306              # Windows API访问（仅Windows）

# ==================== 修复和补充依赖 ====================
pydantic==2.5.0           # 数据验证（reply_manager.py需要）
pyyaml==6.0.1             # YAML支持
loguru==0.7.2             # 日志系统
colorama==0.4.6           # 跨平台颜色支持